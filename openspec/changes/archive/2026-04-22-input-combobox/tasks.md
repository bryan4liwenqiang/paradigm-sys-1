## 1. 组合框输入组件与选项目录

- [x] 1.1 在前端定义关键字段的选项目录与字段元数据（字段 key、默认模式、候选值、兜底手动输入）。
- [x] 1.2 将启动区关键输入从纯文本改为“下拉选择 + 手动输入切换”组合控件，并保留现有默认值行为。
- [x] 1.3 为每个双模式字段补充模式提示与切换交互，避免用户误解当前输入来源。

## 2. 状态管理与请求组装

- [x] 2.1 扩展前端状态模型，保存每个字段的输入模式与值（选项值或手动值）。
- [x] 2.2 更新会话启动请求组装逻辑，使双模式输入统一序列化并保持老字段兼容。
- [x] 2.3 调整本地草稿持久化与恢复，确保页面刷新后模式与值都能正确回填。

## 3. 后端兼容与规范化

- [x] 3.1 在会话创建参数解析中兼容选项值与手动值输入路径，不破坏现有请求格式。
- [x] 3.2 增加输入规范化处理（trim、空值清理、基础映射），降低上下文噪声。
- [x] 3.3 对解析异常或字段配置缺失场景提供降级行为（回退纯文本）并记录可追踪告警。

## 4. 与现有会话引擎协同

- [x] 4.1 确保标准化选项值与自定义值都能被缺口识别与追问逻辑稳定消费。
- [x] 4.2 验证双模式输入不会影响现有阶段推进、解释性输出与方法推荐主流程。

## 5. 验证与回归

- [x] 5.1 执行健康检查与会话冒烟：`curl /health`、`POST /api/v1/sessions/start`（选项模式）。
- [x] 5.2 验证手动输入模式：关键字段使用自定义值仍可成功创建并推进会话。
- [x] 5.3 验证兼容性：仅旧文本字段请求仍可正常工作（不要求前端同步升级）。
