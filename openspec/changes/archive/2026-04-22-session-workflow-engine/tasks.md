## 1. 输入模型扩展（后端）

- [x] 1.1 为会话创建请求增加结构化上下文字段解析（保持 `businessRequest` 兼容必填）。
- [x] 1.2 将结构化字段持久化到 `SessionState` 上下文，并确保旧会话数据可兼容读取。
- [x] 1.3 为缺失关键结构化字段建立“缺口标识”，供追问逻辑使用。

## 2. 动态步骤编排与完成判定

- [x] 2.1 为步骤定义补充“最小信息要求”与条件分支元数据。
- [x] 2.2 实现步骤完成判定：信息不足保持 `CURRENT` 并生成补充追问。
- [x] 2.3 实现条件分支步骤插入与状态追踪（与主步骤统一状态机）。

## 3. 阶段解释性信息输出

- [x] 3.1 扩展会话查询响应，输出阶段目标、已确认事实、关键缺口、下一步建议、未决项。
- [x] 3.2 在阶段完成切换时输出结构化阶段摘要与未决项。
- [x] 3.3 在无 `OPENROUTER_API_KEY` 场景下提供解释字段的规则化降级文本。

## 4. 前端展示与交互调整

- [x] 4.1 在启动区域增加结构化输入项（可选字段）并与现有表单兼容。
- [x] 4.2 在阶段视图渲染新增解释信息区块（目标/事实/缺口/建议/未决项）。
- [x] 4.3 在“信息不足不推进”时展示明确的补充提示，避免用户误以为流程卡死。

## 5. 验证与回归

- [x] 5.1 执行健康检查与会话冒烟测试：`curl /health`、`POST /api/v1/sessions/start`、`POST /api/v1/sessions/{id}/message`。
- [x] 5.2 验证旧请求兼容：仅传 `businessRequest` 仍可创建并推进会话。
- [x] 5.3 验证降级行为：未配置 `OPENROUTER_API_KEY` 时解释字段与追问流程仍可用。
- [x] 5.4 验证健康检查回退策略：在 `GET /health` 不可达时，系统应返回可判定失败并记录错误原因，并可通过 `/health2` 回退探针返回一致失败语义。